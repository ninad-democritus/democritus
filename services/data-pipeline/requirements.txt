fastapi==0.114.2
uvicorn[standard]==0.30.6
requests==2.32.3
pydantic>=2.6.0,<3.0.0  # Aligned with other services
pandas==2.1.4
pyarrow>=9.0.0,<14.0.0
openpyxl==3.1.2
minio==7.2.10

# PySpark and Iceberg dependencies
pyspark==3.5.0
pyiceberg[s3fs,pyarrow]==0.6.1
# pynessie==0.67.0  # Compatibility issues, using REST API directly

# AWS SDK dependencies for Iceberg S3 integration
boto3==1.34.162
botocore==1.34.162
# s3fs version compatible with pyiceberg fsspec requirements
s3fs>=2023.1.0,<2024.1.0

# Additional data processing libraries  
# sqlalchemy==2.0.25  # Commented out due to conflict with OpenMetadata

# OpenMetadata integration (upgraded for pydantic 2.x compatibility)
openmetadata-ingestion>=1.5.0,<1.6.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0


