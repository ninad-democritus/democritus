/**
 * API Configuration
 * Defines the base URL for backend API calls
 */
export const API_CONFIG = {
  // In production, this would be the same origin
  // For development with separate MFE ports, we need to specify port 80 (main nginx)
  baseUrl: 'http://localhost'
};


